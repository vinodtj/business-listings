# Netlify Configuration - Minimal Strategy (Strategy 3)
# Using Node.js 22.x (LTS) as recommended by Netlify

[build]
  command = "npm install && npx prisma generate && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"

[[plugins]]
  package = "@netlify/plugin-nextjs"
